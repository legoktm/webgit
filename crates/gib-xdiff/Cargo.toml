[package]
name = "gib-xdiff"
version = "0.0.0"
edition = "2024"
publish = false
# The vendored library is LGPL-2.1-or-later
license.workspace = true
links = "gib_xdiff"

[dependencies]

# Only the wasm build compiles the `memchr` shim in `src/shim.rs`
[target.'cfg(target_arch = "wasm32")'.dependencies]
memchr.workspace = true

[build-dependencies]
cc.workspace = true

[dev-dependencies]
tempfile.workspace = true
