[package]
name = "gib-diff-delta"
version = "0.0.0"
edition = "2024"
publish = false

# Links Git source code, which is GPL-2.0-only.
license = "GPL-2.0-only"
links = "gib_diff_delta"

[dependencies]

[build-dependencies]
cc.workspace = true
